class Solution {
public:
    int maximumGroups(vector<int>& grades) {
        int n=grades.size();
        int a=1;
        while((1+a)*a/2<=n)
        {
            a++;
        }
        return a-1;
        
    }
};
